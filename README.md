# Flyn Use WPCom Libraries

Replaces all wp-includes/wp-admin script/style URLs with the wordpress.com equivalent to take load off the local server.

## Installation

* `git clone` into */wp-content/plugins/flyn-use-wpcom-libraries* directory
